# experiments

A sandbox repo for product and interaction experiments.

## Current demos

- `chat/` — OpenCloud-style multi-agent chat workspace demo

## Run locally

```bash
npm install
npm run dev
```

This repo uses Vite with `chat/` as the app root.
